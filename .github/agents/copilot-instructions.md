# fabric-spec Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-02-26

## Active Technologies
- Python 3.11 (PySpark kernel, Fabric Runtime 1.3) + `msgraph-sdk` 1.55.0 (Graph v1.0), `msgraph-beta-sdk` 1.56.0 (Graph beta), `azure-identity` (auth), `pyspark` (built-in), `delta-spark` (built-in), `fabric-cicd` 0.2.0 (deployment), `ms-fabric-cli` 1.4.0 (provisioning) (001-copilot-license-usage)
- Microsoft Fabric Lakehouse — Delta Lake format exclusively (Constitution P-IV) (001-copilot-license-usage)

- Python 3.11 (PySpark kernel, Fabric Runtime 1.3) + `msal` (Graph auth), `requests` (HTTP client), `pyspark` (built-in), `delta-spark` (built-in), `fabric-cicd` 0.2.0 (deployment), `ms-fabric-cli` 1.4.0 (provisioning) (001-copilot-license-usage)

## Project Structure

```text
src/
tests/
```

## Commands

cd src [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] pytest [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] ruff check .

## Code Style

Python 3.11 (PySpark kernel, Fabric Runtime 1.3): Follow standard conventions

## Recent Changes
- 001-copilot-license-usage: Added Python 3.11 (PySpark kernel, Fabric Runtime 1.3) + `msgraph-sdk` 1.55.0 (Graph v1.0), `msgraph-beta-sdk` 1.56.0 (Graph beta), `azure-identity` (auth), `pyspark` (built-in), `delta-spark` (built-in), `fabric-cicd` 0.2.0 (deployment), `ms-fabric-cli` 1.4.0 (provisioning)
- 001-copilot-license-usage: Added Python 3.11 (PySpark kernel, Fabric Runtime 1.3) + `msgraph-sdk` 1.55.0 (Graph v1.0), `msgraph-beta-sdk` 1.56.0 (Graph beta), `azure-identity` (auth), `pyspark` (built-in), `delta-spark` (built-in), `fabric-cicd` 0.2.0 (deployment), `ms-fabric-cli` 1.4.0 (provisioning)

- 001-copilot-license-usage: Added Python 3.11 (PySpark kernel, Fabric Runtime 1.3) + `msal` (Graph auth), `requests` (HTTP client), `pyspark` (built-in), `delta-spark` (built-in), `fabric-cicd` 0.2.0 (deployment), `ms-fabric-cli` 1.4.0 (provisioning)

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
